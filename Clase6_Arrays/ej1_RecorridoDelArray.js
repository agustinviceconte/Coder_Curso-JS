const numeros=[1,2,3,4,5];
for(let i=0; i < numeros.length; i ++){
    document.write(i + ")" + numeros[i] +"<br>");

}

console.log(numeros.toString()); //Devuelve una copia del array separado por ","
console.log(numeros);